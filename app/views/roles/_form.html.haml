= simple_form_for(@role) do |f|
  = f.error_notification
  = f.input :name
  = f.input :description, :as => :text
  = f.association :bproce, :label => 'Участвует в процессе'
  = link_to '>>', edit_bproce_path(@role.bproce)
  = f.error :base
  = f.button :submit

