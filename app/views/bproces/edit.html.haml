%h1 Процесс 

= render 'form'

%p
= render 'business_roles'
= render 'documents'
= render 'bapps'
= render 'workplaces'
= render 'iresources'

.button
  = link_to 'Show', @bproce
.button
  = link_to 'Back', bproces_path
