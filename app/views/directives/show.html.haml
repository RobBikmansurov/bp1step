%h2
  = @directive.title
  = @directive.body
  %b
    №
    = @directive.number
    от 
    = @directive.approval
%h3
  = @directive.name
-if !@directive.note.empty?
  %h4
    примечание:
    = @directive.note
%p
- if @directive.document_directive.count > 0
  %h4 На основании этого документа разработаны следующие внутренние документы банка:
  - @directive.document.each do |document|
    %li= link_to document.name, document_path(document)

%p
-if can? :update, @directive
  .button
    = link_to 'Edit', edit_directive_path(@directive) |
.button
  = link_to 'Back', directives_path
