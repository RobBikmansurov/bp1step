-title "Действия пользователей"
-@activities.each do |activity|
  %br
  =activity.created_at.strftime('%d.%m.%Y %H:%M:%S')
  -#=debug activity.inspect
  =link_to activity.owner.displayname, activity.owner if activity.owner
  =render_activity(activity)

.apple_pagination
  = will_paginate @activities