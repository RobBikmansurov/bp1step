-title "Документы процесса " + @bp.shortname
%h2
  Документы процесса
  = link_to @bp.name, bproce_path(@bp)

-#= form_tag documents_path, :method => 'get' do
%table
  %tr
    %th 
    %th Наименование
    %th Описание
    -#    %th Имя файла
    -#%th authority
    %th Статус
    %th Дата
    -#%th responsible
    %th Место
    
  - @documents.each do |document|
    %tr
      %td= document.part
      %td= link_to document.name, document_path(document)
      %td= format_content(document.description)
      -#      %td= document.filename
      %td= document.status
      %td= document.approved 
      %td= document.place
