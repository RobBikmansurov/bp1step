-if activity.trackable
  = 'связал (#' + activity.trackable.id.to_s + '): рабочее место ['
  = link_to activity.trackable.workplace.designation, workplace_path(activity.trackable.workplace)
  ] с процессом
  = link_to activity.trackable.bproce.name, bproce_path(activity.trackable.bproce)
-else
  связь рабочего места с процессом
  \#
  = activity.trackable_id
  уже удалена