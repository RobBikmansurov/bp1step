изменил исполнителю рабочее место
-if activity.trackable
  = '(#' + activity.trackable.id.to_s + '):'
  =link_to activity.trackable.user.displayname, user_path(activity.trackable.user)
  \->
  = link_to activity.trackable.workplace.designation, workplace_path(activity.trackable.workplace)
-else
  \#
  = activity.trackable_id
  уже удален